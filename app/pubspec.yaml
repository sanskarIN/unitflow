name: unitflow
version: 0.1.0+1
publish_to: "none"
description: Offline-first Flutter frontend for the UnitFlow converter.

repository: https://github.com/sanskarIN/unitflow

environment:
  sdk: ">=3.8.0 <4.0.0"

flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
