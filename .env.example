# UnitFlow core features require no secrets or network configuration.
#
# Keep this file as the documented environment boundary. If a future optional
# online feature needs configuration, add placeholder variable NAMES only and
# document whether each value is public configuration or a secret.
#
# Example placeholder format (do not put real values here):
# UNITFLOW_OPTIONAL_SERVICE_BASE_URL=https://example.invalid
